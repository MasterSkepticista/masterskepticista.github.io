My portfolio.

<!-- Setup:
* Install build essentials: `sudo apt install build-essential gcc make`
* Set bundle path: `bundle config set --local path 'vendor/bundle'`
* Run: `bundle install`
* Serve: `bundle exec jekyll serve -l -H localhost` -->
